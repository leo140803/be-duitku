services:
  - type: web
    name: go-api
    env: go
    plan: free
    buildCommand: go build -o server ./cmd
    startCommand: ./server
    autoDeploy: true
    envVars:
      - key: PORT
        value: 10000
